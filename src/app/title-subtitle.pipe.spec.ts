import { TitleSubtitlePipe } from './title-subtitle.pipe';

describe('TitleSubtitlePipe', () => {
  it('create an instance', () => {
    const pipe = new TitleSubtitlePipe();
    expect(pipe).toBeTruthy();
  });
});
